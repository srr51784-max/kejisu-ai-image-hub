# kejisu 部署图文步骤简版

## 1. Supabase
创建项目 → SQL Editor → 粘贴并执行 `supabase/schema.sql`

## 2. Vercel
New Project → 导入 GitHub 仓库 → 添加环境变量 → Deploy

## 3. 设置管理员
先打开网站注册 `kejisu110@gmail.com`，再回 Supabase 执行：
```sql
update public.profiles set is_admin = true where email = 'kejisu110@gmail.com';
```

## 4. 测试
普通用户注册 → 充值页上传付款截图 → 管理员 `/admin` 审核 → 用户获得点数 → `/generate` 生成图片。
